.env
__pycache__/
*.pyc
.venv/
venv/
reports/*.pdf
.DS_Store

# Generated locally / read from the live GitHub Pages deployment at runtime
# (see REMOTE_HISTORY_URL) — never the source of truth, shouldn't be committed.
data/analysis.json
